# Infrastructure & QA Automation Utilities 🐍

![Python](https://img.shields.io/badge/Language-Python_3-blue?style=for-the-badge&logo=python&logoColor=white)
![Linux](https://img.shields.io/badge/OS-Linux-FCC624?style=for-the-badge&logo=linux&logoColor=black)
![Status](https://img.shields.io/badge/Status-Stable-green?style=for-the-badge)

Набор Python-утилит для автоматизации задач системного администрирования, мониторинга ресурсов и валидации данных.
Проект демонстрирует работу с файловой системой, системными вызовами и хешированием на уровне стандартной библиотеки Python (Standard Library).

## 🛠 Описание инструментов

### 1. `data_integrity.py` (File Integrity Monitor)
Скрипт для проверки целостности файлов через хеширование.
- **Задача:** Обнаружение "тихого" повреждения данных (bit rot) при передаче или хранении.
- **Реализация:** Вычисление SHA-256 хеша. Используется **поблочное чтение (Chunking)** по 4096 байт для эффективной работы с файлами большого объема (Large Files) без перегрузки RAM.
- **Use Case:** Валидация бэкапов и миграции данных в СХД.

### 2. `disk_check.py` (Resource Monitoring)
Монитор свободного места на диске с возможностью настройки порогов (Thresholds).
- **Задача:** Предотвращение сбоев тестового стенда из-за нехватки места (`No space left on device`).
- **Реализация:** Вызов системной команды `df -h` через модуль `subprocess`. Парсинг stdout и сравнение процентов занятого места.
- **Use Case:** Pre-flight check перед запуском нагрузочных тестов.

### 3. `log_parser.py` (Log Analyzer)
Утилита для быстрого поиска критических ошибок в логах.
- **Задача:** Автоматизация разбора логов после инцидентов.
- **Реализация:** Построчное чтение файла с использованием Context Managers (`with open`). Поддержка кодировки `utf-8` и обработка исключений.
- **Use Case:** Поиск паттернов `ERROR`, `CRITICAL`, `panic` в системных журналах `/var/log/syslog`.

## 🚀 Как запустить

**Требования:** Python 3.6+ (Внешние библиотеки не требуются).

### Проверка диска:
```bash
python3 disk_check.py
```
### Проверка целостности данных (Демонстрация с порчей файла):
```bash
python3 data_integrity.py
```

---

## ⚙️ Стек технологий
Core: Python 3 (subprocess, hashlib, os, sys)

OS: Linux (Ubuntu/CentOS), macOS

## 🔜 Планы по развитию
 [ ] Добавить отправку алертов в Telegram/Email при обнаружении ошибок.

 [ ] Реализовать подключение по SSH (paramiko) для проверки удаленных узлов.
 
 [ ] Добавить многопоточность для проверки больших массивов файлов.
